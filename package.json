{
  "name": "json_server",
  "version": "1.0.0",
  "description": "demo how to user json-server",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data && node main.js",
    "generate-data": " node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "SangTrandev",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.1",
    "query-string": "^8.1.0",
    "@faker-js/faker": "^7.6.0"
  },
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "nodemon": "^2.0.20",
    "prettier": "2.8.1"
  }
}
